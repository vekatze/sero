import {
  this.rule.get-handle {get-handle},
  this.rule.put-handle {put-handle},
}

data marshal(i, o) {
| Marshal(
    encode: (h: &put-handle, value: &i) -> unit,
    decode: (h: &get-handle) -> ?meta o,
  )
}

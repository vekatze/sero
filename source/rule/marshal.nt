import {
  this.rule.decode-handle {decode-handle},
  this.rule.encode-handle {encode-handle},
}

data marshal(a, b) {
| Marshal(
    encode: (h: &encode-handle, value: &a) -> unit,
    decode: (h: &decode-handle) -> ?meta b,
  )
}

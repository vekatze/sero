import {
  this.rule.get-handle {decode-handle},
  this.rule.encode-handle {encode-handle},
}

data marshal(i, o) {
| Marshal(
    encode: (h: &encode-handle, value: &i) -> unit,
    decode: (h: &decode-handle) -> ?meta o,
  )
}

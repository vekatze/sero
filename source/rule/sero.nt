import {
  this.rule.get-handle {get-handle},
  this.rule.put-handle {put-handle},
}

data sero(i, o) {
| Sero(
    put: (h: &put-handle, value: &i) -> unit,
    get: (h: &get-handle) -> ?meta o,
  )
}

import {
  core.int.show {show-int},
  this.move.common.get {get-int32},
  this.move.common.put {put-int32},
  this.move.decode {decode},
  this.move.encode {encode},
  this.rule.get-handle {get-handle},
  this.rule.marshal {Marshal, marshal},
  this.rule.put-handle {put-handle},
}

define _put(h: &put-handle, x: &int32): unit {
  put-int32(h, *x)
}

define _get(h: &get-handle): ?meta int32 {
  try v = get-int32(h) in
  Right(quote {v})
}

inline int32-marshal: marshal(int32, int32) {
  Marshal of {
    put = _put,
    get = _get,
  }
}

define zen(): unit {
  pin val = 14909826 in
  match decode(int32-marshal, encode(int32-marshal, val, 8)) {
  | Left(_) =>
    print("left")
  | Right(v) =>
    printf("value: {}\n", [show-int(sext-int32-int(v))])
  }
}

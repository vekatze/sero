import {
  core.binary {binary},
  this.get-kit {get-binary, get-kit},
  this.put-kit {put-binary, put-kit},
  this.sero {Sero, sero},
}

define _put(k: &put-kit, x: &binary) -> unit {
  put-binary(k, x)
}

define _get(k: &get-kit) -> ?+binary {
  try b = get-binary(k);
  Right(lift {b})
}

constant binary-sero: sero(binary) {
  Sero{
    put := _put,
    get := _get,
  }
}

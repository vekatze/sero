import {
  core::int.io {print-int-line},
  decode {decode},
  encode {encode},
  instance.bool {bool-sero},
  instance.either {either-sero},
  instance.int64 {int64-sero},
  sero {sero},
}

constant _either-int-bool: sero(either(int, bool)) {
  either-sero(int64-sero, bool-sero)
}

define zen() -> unit {
  pin val = Left(-1234);
  match decode(_either-int-bool, encode(_either-int-bool, val, 8)) {
  | Left(_) =>
    print("error\n")
  | Right(x-or-y) =>
    match x-or-y {
    | Left(x) =>
      print("int: ");
      print-int-line(x);
    | Right(_) =>
      print("bool\n")
    }
  }
}

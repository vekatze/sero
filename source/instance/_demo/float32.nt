import {
  core.float.io {print-float-line},
  this.decode {decode},
  this.encode {encode},
  this.instance.float32 {float32-sero},
}

define zen() -> unit {
  pin val = -3.8;
  match decode(float32-sero, encode(float32-sero, val, 8)) {
  | Left(_) =>
    print("left")
  | Right(v) =>
    print("value: ");
    print-float-line(fpext-float32-float(v), 6);
  }
}

import {
  this.decode {decode},
  this.encode {encode},
  this.instance.unit {unit-sero},
}

define zen() -> unit {
  pin val = Unit;
  match decode(unit-sero, encode(unit-sero, val, 8)) {
  | Left(_) =>
    print("left")
  | Right(_) =>
    print("right\n")
  }
}
